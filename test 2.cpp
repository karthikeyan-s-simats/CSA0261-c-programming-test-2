#include<stdio.h>
#include<conio.h> 
{
 filter_string(first_string, second_string):
    first_set = set(first_string)
    result = ""
    for characters in second_string:
        if characters not in first_set:
            result += character
    return 
}

