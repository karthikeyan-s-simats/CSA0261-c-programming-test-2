#include<studio.h>
#include<conio.h>
int main[]
{
     int arr[50], i, j, flag;
     printf(%d,n)
    //initialize the array with numbers from 1 to 50
    for (i = 0; i < 50; i++)
        arr[i] = i + 1; 
           int arr[50], i, j, flag;
       }
   
    // Loop through the array to check for composite numbers
    for (i = 0; i < 50; i++) {
        // Skip checking if the number is 1 or a prime number
        if the number is composite print it 
     if (is_composite == 1) {
            printf("%d ", arr[i]);
        }
    return 0;

    }














    






